<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
</head>
<body>
    <% if (typeof user === 'undefined' || !user) { %>
        <!-- Render login option if user is not authenticated -->
        <a href="/auth/login">Login</a>
      <% } else { %>
        <!-- Render user name if user is authenticated -->
        <p>Welcome, <%= user.name %></p>
        <a href="/users/showUsers">Show users</a>
        <br><br>
        <a href="/patrimonies/showPatrimonies">Show patrimonies</a>
        <br><br>
        <a href="/auth/logout">Logout</a>
      <% } %> 
      
</body>
</html>